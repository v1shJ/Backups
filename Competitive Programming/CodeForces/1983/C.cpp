#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    while(n--){
        int t; cin >> t;
        int alice[t];
        for(int c = 0; c < 3; c++){
            cin >> alice[c];
        }
        int bob[t];
        for(int c = 0; c < 3; c++){
            cin >> bob[c];
        }
        int charlie[t];
        for(int c = 0; c < 3; c++){
            cin >> charlie[c];
        } 

        
    }
}